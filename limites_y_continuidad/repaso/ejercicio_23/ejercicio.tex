\item Sea
    $$g(x) = \begin{cases} 
    2x - x^2 & \text{si } 0 \le x \le 2 \\ 
    2 - x & \text{si } 2 < x \le 3 \\ 
    x - 4 & \text{si } 3 < x < 4 \\ 
    \pi & \text{si } x \ge 4 
    \end{cases}$$
    \begin{enumerate}
        \item Para cada uno de los números 2, 3 y 4, determine si $g$ es continua por la izquierda, continua por la derecha, o continua en el número.
        \item Trace la gráfica de $g$.
    \end{enumerate}
