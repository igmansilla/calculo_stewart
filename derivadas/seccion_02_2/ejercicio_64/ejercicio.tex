\item Determine en qué números la función $g$ dada por
$$g(x) = \begin{cases} 
-1 - 2x & \text{si } x < -1 \\ 
x^2 & \text{si } -1 \le x \le 1 \\ 
x & \text{si } x > 1 
\end{cases}$$
es diferenciable. Proporcione una fórmula para $g'$ y trace las gráficas de $g$ y $g'$.
