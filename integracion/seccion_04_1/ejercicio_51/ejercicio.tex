\item Pruebe la fórmula de la suma de una serie geométrica finita que tiene como primer término $a$ y como razón (o cociente) común $r$:
$$\sum_{i=1}^n ar^{i-1} = a + ar + ar^2 + \cdots + ar^{n-1} = \frac{a(r^n - 1)}{r - 1}$$
