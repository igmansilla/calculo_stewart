\item Sea $f_n(x) = (\sen nx)/n^2$. Demuestre que la serie $\sum f_n(x)$ converge para todo valor real de $x$ pero la serie de derivadas $\sum f_n'(x)$ diverge cuando $x = 2n\pi$, en donde $n$ es un entero. Determine para qué valores de $x$ la serie $\sum f_n''(x)$ converge.
